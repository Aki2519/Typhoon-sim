# 台风路径模拟系统 · 使用说明

> 面向第一次使用的用户。所有界面文字都是中文；本文说明每个界面能做什么、按哪个键。
> 如果你看到的是**英文图表标题**，那是「架空模拟器」（`simulator/`）导出的科研图，见第 8 节。

---

## 1. 运行

```
pip install -r requirements.txt
python main.py
```

- 首次启动会加载台风数据（进度条显示 `已加载/总数`），随后出现主界面。
- 台风数据放在 `typhoon/` 目录（可分子目录）。支持两种文本格式：
  - **JTWC 原始格式**（`*.dat`，含 BEST/CARQ 等列）：首次保存时会自动生成同名 `*_ty.txt` 副本，之后编辑写在副本上，原始文件不动。
  - **simple-bdeck 格式**（`*.txt`，逗号分隔，纬度列如 `205N`、经度列如 `1300E`）。
- 配置文件 `config.json` 记录窗口尺寸、图标集、地图范围等；删掉它会恢复默认（并出现一次首启引导条）。

## 2. 主界面速览

| 区域 | 内容 |
|------|------|
| 中间大图 | 地图 + 台风路径/图标/信息框 |
| 顶部或底部功能栏 | 模式、播放、速度、台风列表、点列表、设置、绘画、镜头跟踪等按钮 |
| 左上角圆环钟 | 模拟时间（风季/模拟模式）；正常/编辑模式在左下角 |
| 台风信息框 | 名称、时间、位置、风速/气压、移速、巅峰、ACE；样式可在设置里逐行自定义 |

**鼠标**：右键拖拽平移地图；滚轮缩放；左键在编辑模式下选点/拖点。

## 3. 四种模式（`H` 循环切换；模拟模式从功能栏进入）

| 模式 | 用途 |
|------|------|
| **正常** | 逐条回放单个台风（`[` `]` 切换、自动连播），适合看某个台风的完整生命史 |
| **风季** | 一整年所有台风同时推进，含季节时钟、ACE 累计、月度总结弹窗 |
| **编辑** | 增删改报点、新建台风（`I`）、拖点、Ctrl+方向键微调、撤销/重做 |
| **模拟** | 架空台风模拟（数值内核）：生成台风、图层叠加（气压/风场/云图/SST/湿度/热含量/路径/副高）、粒子风场 |

## 4. 键盘快捷键（完整表也在「设置 → 快捷键」面板里）

| 按键 | 功能 |
|------|------|
| `Space` | 播放 / 暂停 |
| `H` | 切换模式（正常 / 风季 / 编辑） |
| `[` `]` | 上一个 / 下一个台风 |
| `←` `→` | 减速 / 加速；`X` 速度重置 |
| `K` | 强度折线图 / ACE 图表 |
| `G` | 点列表（编辑模式可编辑，正常模式只读） |
| `O` | 台风列表 |
| `J` | 脚本（选择 / 编辑 / 运行） |
| `T` | 时间跳转（风季模式） |
| `S` | 设置 |
| `R` | 重置地图视图 |
| `P` | 截图 |
| `D` | 打开绘画面板（正常/风季/编辑模式） |
| `F1` | 隐藏 / 显示界面（截图用） |
| `F12` | 窗口置顶 |
| `I` | 编辑模式：新建台风 |
| `,` `.` | 编辑模式：上一个 / 下一个报点 |
| `Delete` | 编辑模式：删除选中报点 |
| `Ctrl+Z` / `Ctrl+Y` | 撤销 / 重做 |
| `Ctrl+R` | 重新加载台风数据 |
| `Ctrl+方向键` | 编辑模式：按吸附步长微调选中报点 |

## 5. 编辑台风数据

1. `H` 切到**编辑**模式（或从台风列表双击进入）。
2. `[` `]` 选中要改的台风；`,` `.` 选中报点；左键拖动报点，`Ctrl+方向键` 微调。
3. 保存是**自动**的（拖点/改点即写盘），不会弹「已保存」打扰。
4. 新建：`I` → 填写编号/名称 → 在地图上点位置加第一个报点。
5. 删除报点：选中后 `Delete`（至少保留 3 个点）；删除整条台风：台风列表里操作。

> 路径的「点阵 / 渐变线」样式、平滑开关、点大小都在**设置**里；编辑模式下强制点阵以便拖点。

## 6. 统计与图表

功能栏「统计」里包含：强度对比、路径对比（多台风同图）、ACE 热力图、路径长度、风季总结、多年对比、总结条列表。

- 图表里的 `TD / TS / STS / C1~C5 / EX / SS / SD` 是**国际通用的强度等级缩写**（不是英文界面）：
  - TD 热带低压、TS 热带风暴、STS 强热带风暴、C1~C5 一~五级台风、EX 温带气旋、SS 副热带风暴、SD 副热带低压。
- `ACE` = 累积气旋能量指数（Accumulated Cyclone Energy），数值越大代表台风越强越持久。

## 7. 脚本（`J`）

脚本用缩进表示层级，首行 `# 注释` 作为脚本简介：

```
# 示例:跳到某天并对准某区域
jump 2026-04-12 00            # 时间跳转
target 115 18 w=15            # 镜头对准 (115°E, 18°N), 视野宽度 15°
wait 2                        # 等待 2 秒
repeat 3
    step                        # 重复 3 次: 步进
```

详细命令见对话框内「编辑脚本」的提示；`repeat` 有展开上限（防止误写死循环）。

## 8. 架空模拟器与英文图表

`simulator/` 下是独立的**数值模拟子项目**（SimCore），入口是：

```
python simulator/run.py --seed 1 --years 3             # 界面模式
python simulator/run.py --seed 1 --years 3 --headless  # 批处理(仅生成数据)
```

它导出的**科研图/视频**沿用气象惯例的英文标题与单位（SLP、hPa、kt、dam、IR、VIS…），
文件名与坐标轴标签也是英文——如果你看到「满屏英文」，通常是从这里来的，不是主程序界面。
主程序的模拟模式（功能栏进入）图层名与提示都是中文。

## 9. 常见问题

| 现象 | 原因与处理 |
|------|-----------|
| 台风图标是方块 / 缺失 | `assets/SMCY/`（视频图标，体积大）未随仓库分发。在**设置 → 图标集**里换成内置图标集即可 |
| 启动后地图空白 | 检查 `map/` 下的底图与陆地掩码是否存在；`R` 重置视图试试 |
| 找不到某个台风 | 文件名需要含编号数字（如 `wp132026.dat`）；放错目录或格式错误的数据会被跳过，可看控制台/日志输出 |
| 保存后数据变少 | JTWC 原始文件不会被改写：编辑写在自动生成的 `*_ty.txt` 副本里；重新加载(`Ctrl+R`)后编辑内容仍在 |
| 满屏英文 | 见第 8 节：那是模拟器导出的科研图；主界面是中文 |
| 卡顿 | 设置 → 图标集换掉 SMCY；关闭信息框/网格/ACE 条；台风很多时用风季模式会明显更重 |

## 10. 目录速查

| 路径 | 说明 |
|------|------|
| `main.py` | 主程序入口（回放/统计/编辑） |
| `typhoon/` | 台风数据（可分子目录） |
| `assets/` | 图标、音效、字体、SMCY 素材（SMCY 未入库） |
| `config.json` | 应用配置 |
| `app/` | 主程序源码（界面/渲染/数据/统计） |
| `simulator/` | 架空模拟器子项目（数值内核/图层渲染/WRF 工具） |
| `tests/` | 测试（`python -m pytest tests -q`） |
